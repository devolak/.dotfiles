#!/usr/bin/env bash

chosen=$(printf "Reboot\nShutdown\nLogout" | fuzzel --dmenu -p "Power Menu: " --minimal-lines)

case "$chosen" in
  Reboot) systemctl reboot ;;
  Shutdown) systemctl poweroff ;;
  Logout) niri msg action quit ;;
  *) exit 1 ;;
esac

#!/usr/bin/env bash

chosen=$(printf "Logout\nSuspend\nReboot\nShutdown" | fuzzel --dmenu -p "Power Menu: " --minimal-lines)

case "$chosen" in
Logout) hyprctl dispatch exit ;;
Suspend) systemctl suspend ;;
Reboot) systemctl reboot ;;
Shutdown) systemctl poweroff ;;
*) exit 1 ;;
esac

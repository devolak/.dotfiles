#!/usr/bin/env bash

chosen=$(printf "Audio\nBluetooth\nWifi" | fuzzel --dmenu -p "Quick settings: " --minimal-lines)

case "$chosen" in
  Audio) foot -a tuipopup wiremix ;;
  Bluetooth) foot -a tuipopup bluetui ;;
  Wifi) foot -a tuipopup impala ;;
  *) exit 1 ;;
esac
